export async function consumeCredits(brandId: string, amount: number, _type: string, _note?: string) {
  // Placeholder implementation; integrate with real credit system
  return { ok: true, remaining: Infinity };
}

export async function addCredits(brandId: string, amount: number, _type: string, _note?: string) {
  // Placeholder for adding credits back
  return { ok: true };
}
